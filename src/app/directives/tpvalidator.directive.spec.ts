import { ValidatorDirDirective } from './tpvalidator.directive';

describe('ValidatorDirDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidatorDirDirective();
    expect(directive).toBeTruthy();
  });
});
